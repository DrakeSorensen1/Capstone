/** @type {import('next').NextConfig} */
const nextConfig = {}


module.exports = {
    reactStrictMode: false,
    images: {
        domains: ['images.unsplash.com', 'fakeimg.pl', 'via.placeholder.com', 'bootstrapious.com'],
    }
}
